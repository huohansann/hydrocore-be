# ============================================================
# Shared Redis (Nacos data-id: redis.yml)
# ============================================================

spring:
  redis:
    host: localhost
    port: 6379
    password: root123
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 16
        max-idle: 8
        min-idle: 2
