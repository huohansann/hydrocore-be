# ============================================================
# HydroCore properties bag (Nacos data-id: hydrocore-config.properties)
# Add key=value pairs as needed. The clean baseline can stay nearly empty.
# ============================================================

# app.display-name=HydroCore
# app.env=dev
