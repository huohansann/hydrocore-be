<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.siact.module.permission.mapper.MenuMapper">
    
    <!-- 根据角色ID列表查询菜单ID列表 -->
    <select id="selectMenuIdsByRoleIds" resultType="java.lang.Long">
        SELECT DISTINCT m.menu_id
        FROM sys_role_menu m
        WHERE m.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>
    
    <!-- 根据角色ID列表查询菜单列表 -->
    <select id="selectMenusByRoleIds" resultType="com.siact.module.permission.entity.MenuEntity">
        SELECT DISTINCT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND m.status = 1
        AND m.deleted = 0
        ORDER BY m.id
    </select>
    
</mapper> 