package com.siact.module.permission.service.impl;import com.alibaba.fastjson2.JSONObject;import com.siact.common.utils.JwtUtil;import com.siact.module.permission.dto.LoginRequest;import com.siact.module.permission.entity.UserEntity;import com.siact.module.permission.service.AuthService;import com.siact.module.permission.service.UserService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.context.annotation.Lazy;import org.springframework.security.core.GrantedAuthority;import org.springframework.security.core.userdetails.UserDetails;import org.springframework.security.core.userdetails.UserDetailsService;import org.springframework.security.crypto.password.PasswordEncoder;import org.springframework.stereotype.Service;import javax.annotation.Resource;import javax.servlet.http.HttpServletRequest;import java.util.List;import java.util.stream.Collectors;/** * 登录认证服务实现 * * @author : wr */@Servicepublic class AuthServiceImpl implements AuthService {    @Lazy    @Autowired    private UserDetailsService userDetailsService;    @Autowired    private JwtUtil jwtUtil;    @Resource    private UserService userService;    @Autowired    private PasswordEncoder passwordEncoder;    /**     * 登录     *     * @param request     * @return     */    @Override    public JSONObject login(LoginRequest request) {        UserDetails user = userDetailsService.loadUserByUsername(request.getUsername());        if (passwordEncoder.matches(request.getPassword(), user.getPassword())) {            List<String> roles = user.getAuthorities().stream().map(GrantedAuthority::getAuthority).collect(Collectors.toList());            // 根据用户账号获取用户信息            JSONObject userInfo = getUser(request.getUsername(),roles);            String token = jwtUtil.generateToken(request.getUsername(), userInfo);            JSONObject json = new JSONObject();            json.put("token", "Bearer " + token);            json.put("userInfo", userInfo);            return json;        }        throw new RuntimeException("密码错误");    }    private JSONObject getUser(String username, List<String> roles) {        UserEntity userByUsername = userService.getUserByUsername(username);        if (userByUsername != null) {            JSONObject userInfo = new JSONObject();            userInfo.put("username", userByUsername.getUsername());            userInfo.put("avatar", userByUsername.getAvatar());            userInfo.put("roles", roles);            return userInfo;        }        return null;    }    /**     * 登出     *     * @param request     * @return     */    @Override    public boolean logout(HttpServletRequest request) {        // 实际项目需要实现Token黑名单机制        String token = resolveToken(request);        // 解析出用户名称        String username = jwtUtil.extractUsername(token);        // 删除Token        jwtUtil.deleteToken(username);        return true;    }    /**     * 解析Token     *     * @param request     * @return     */    private String resolveToken(HttpServletRequest request) {        String bearerToken = request.getHeader("Authorization");        // 验证并处理Authorization头格式        if (org.springframework.util.StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")) {            return bearerToken.substring(7);        }        return null;    }}