# EK 温度限制参数 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `callIntelligentInterface()` 中构建 EK 温度限制参数并传递给算法接口。

**Architecture:** 更新 `controlTargetPoints` 数据格式解析，新增 `buildEKParams` 辅助方法构建 EK 结构（EKL/EKH/EKS），替换占位符代码。

**Tech Stack:** Java 17, Spring Boot, fastjson2, MyBatis-Plus

---

### Task 1: 更新 controlTargetPoints 解析并适配新数据格式

**Files:**
- Modify: `src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java:84-90`

- [ ] **Step 1: 更新 cptData 类型转换和 dataCode 提取逻辑**

将第 84-90 行从：

```java
        // 添加温度设定值参数
        SysConfigDTO controlTargetPoints = sysConfigService.getByCode(SysConfigCodeConstants.CONTROL_TARGET_POINTS);
        Map<String, String> cptData =(Map<String, String>) controlTargetPoints.getData();
        List<ControlIntervalConfigDTO> targetTemps = configService.selectListByDataCodeList(new ArrayList<>(cptData.values()));
        for (ControlIntervalConfigDTO dto : targetTemps) {
          params.put(dto.getMeasurePoint() + "_SP", new BigDecimal(dto.getTemperatureSet()));
        }
```

改为：

```java
        // 添加温度设定值参数
        SysConfigDTO controlTargetPoints = sysConfigService.getByCode(SysConfigCodeConstants.CONTROL_TARGET_POINTS);
        Map<String, Map<String, Object>> cptData = (Map<String, Map<String, Object>>) controlTargetPoints.getData();
        List<String> dataCodeList = cptData.values().stream()
                .map(v -> MapUtils.getString(v, "code"))
                .toList();
        List<ControlIntervalConfigDTO> targetTemps = configService.selectListByDataCodeList(dataCodeList);
        for (ControlIntervalConfigDTO dto : targetTemps) {
          params.put(dto.getMeasurePoint() + "_SP", new BigDecimal(dto.getTemperatureSet()));
        }
```

需要新增 import（如果尚未存在）：
```java
import java.util.stream.Collectors;
```

> 注意：`.toList()` 是 Java 16+ 内置方法。如果项目 Java 版本低于 16，改用 `.collect(Collectors.toList())`。

- [ ] **Step 2: 验证编译通过**

Run: `cd /home/Tso/devroot/code/projects/kic-be && mvn compile -pl . -q 2>&1 | tail -5`
Expected: BUILD SUCCESS

- [ ] **Step 3: 提交**

```bash
git add src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java
git commit -m "refactor(algorithm): 更新 controlTargetPoints 解析适配新数据格式"
```

---

### Task 2: 新增 buildEKParams 辅助方法

**Files:**
- Modify: `src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java` (新增方法)

- [ ] **Step 1: 在 `getValueInJson` 方法之后（约第 203 行）添加 `buildEKParams` 方法**

```java
    @SuppressWarnings("unchecked")
    private Map<String, Map<String, BigDecimal>> buildEKParams(Map<String, Map<String, Object>> cptData, List<ControlIntervalConfigDTO> targetTemps) {
        // 按 measurePoint 建立 targetTemps 索引
        Map<String, ControlIntervalConfigDTO> tempMap = new HashMap<>();
        for (ControlIntervalConfigDTO dto : targetTemps) {
            tempMap.put(dto.getMeasurePoint(), dto);
        }

        Map<String, Map<String, BigDecimal>> ek = new LinkedHashMap<>();
        for (Map.Entry<String, Map<String, Object>> entry : cptData.entrySet()) {
            String pointName = entry.getKey();
            Map<String, Object> pointConfig = entry.getValue();

            BigDecimal ekl = new BigDecimal(MapUtils.getString(pointConfig, "ekl"));
            ControlIntervalConfigDTO tempConfig = tempMap.get(pointName);
            if (tempConfig == null) {
                log.warn("EK参数构建: 未找到点位 {} 对应的温度控制配置", pointName);
                continue;
            }

            Map<String, BigDecimal> limitMap = new LinkedHashMap<>();
            limitMap.put("EKL", ekl);
            limitMap.put("EKH", new BigDecimal(tempConfig.getLowControl()));
            limitMap.put("EKS", new BigDecimal(tempConfig.getLowAlarm()));
            ek.put(pointName, limitMap);
        }
        return ek;
    }
```

- [ ] **Step 2: 验证编译通过**

Run: `cd /home/Tso/devroot/code/projects/kic-be && mvn compile -pl . -q 2>&1 | tail -5`
Expected: BUILD SUCCESS

- [ ] **Step 3: 提交**

```bash
git add src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java
git commit -m "feat(algorithm): 新增 buildEKParams 辅助方法构建温度限制参数"
```

---

### Task 3: 替换占位符并清理 TODO 注释

**Files:**
- Modify: `src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java:92-119`

- [ ] **Step 1: 删除 TODO 注释块（第 92-118 行）和占位符代码（第 119 行），替换为实际调用**

将第 92-119 行整块（从 `// TODO: 新增功能` 到 `params.put("EK", "数据");`）替换为：

```java
        // 构建温度限制参数 EK
        params.put("EK", buildEKParams(cptData, targetTemps));
```

替换后该区域完整代码应为：

```java
        for (ControlIntervalConfigDTO dto : targetTemps) {
          params.put(dto.getMeasurePoint() + "_SP", new BigDecimal(dto.getTemperatureSet()));
        }

        // 构建温度限制参数 EK
        params.put("EK", buildEKParams(cptData, targetTemps));

        Object cacheObject = redis.getCacheObject(AlgorithmConstant.INTELLI_ALGORITHM_CACHE_KEY);
```

- [ ] **Step 2: 验证编译通过**

Run: `cd /home/Tso/devroot/code/projects/kic-be && mvn compile -pl . -q 2>&1 | tail -5`
Expected: BUILD SUCCESS

- [ ] **Step 3: 提交**

```bash
git add src/main/java/com/siact/module/algorithm/services/impl/IntelligentDataServiceImpl.java
git commit -m "feat(algorithm): 集成 EK 温度限制参数到算法调用"
```